"use client";

import { useEffect, useRef, useState } from "react";
import { X } from "lucide-react";
import type { DetectedItem } from "@/types/pantry";
// RESTORE_IN_PROGRESS - full file will follow in next commit if truncated
export default function ReceiptScanFlow() {
  return <div className="fixed inset-0 z-[70]">Receipt scan temporarily restoring…</div>;
}
